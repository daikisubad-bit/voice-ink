# VoiceInk マニュアル

## VoiceInk とは

音声を録音して自動で文字起こし・AI整形し、入力欄に挿入できる Chrome 拡張機能です。

- 音声録音 → Groq Whisper で文字起こし → Claude AI で整形 → 入力欄に挿入
- Chatwork・Google ドキュメント・Frame.io など、ほぼすべてのサイトで使えます

---

## インストール手順（初回のみ）

### 1. GitHub Desktop をインストール
- `https://desktop.github.com` からダウンロードしてインストール
- GitHub アカウントでサインインする

### 2. リポジトリをクローン（初回のみ）
1. GitHub Desktop を開く
2. メニュー「File」→「Clone repository...」
3. 「GitHub.com」タブで `voice-ink` を選択
4. 「Clone」をクリック

### 3. Chrome に拡張機能を読み込む（初回のみ）
1. Chrome で `chrome://extensions` を開く
2. 「パッケージ化されていない拡張機能を読み込む」をクリック
3. クローンしたフォルダの中の `extension` フォルダを選択して「開く」

### 4. APIキーを設定（初回のみ）
1. Chrome 右上の拡張機能アイコン（パズルピース）→ VoiceInk をクリック
2. Groq API キーと Anthropic API キーを入力して「保存」

### 5. ショートカットキーを設定（初回のみ）
1. `chrome://extensions/shortcuts` を開く
2. VoiceInk の各項目の鉛筆アイコンをクリックしてキーを入力
   - 「VoiceInkを開く/閉じる」→ `Cmd+Shift+Y`
   - 「録音開始/停止」→ `Cmd+Shift+K`

---

## 毎回の更新手順

新しい機能やバグ修正が入ったときの更新手順です。**3ステップを必ず順番通りに行ってください。**

1. **GitHub Desktop** で「Pull origin」ボタンをクリック
2. **`chrome://extensions`** で VoiceInk の更新ボタン（🔄）をクリック
3. **使っているページ**で `Cmd+R` を押してリロード

> ⚠️ **3番を忘れると古いバージョンのままになります。** 拡張機能を更新しても、開いているタブには自動で反映されません。

---

## ショートカットキー一覧

| キー | 動作 |
|------|------|
| `Cmd+Shift+Y` | VoiceInk パネルを開く/閉じる |
| `Cmd+Shift+K` | 録音開始/停止 |

---

## 基本的な使い方

### 音声入力の流れ
1. `Cmd+Shift+Y` でパネルを開く
2. `Cmd+Shift+K` で録音開始（または録音ボタンをクリック）
3. 話す
4. `Cmd+Shift+K` で録音停止
5. 自動で文字起こし → AI整形が行われる
6. 「入力欄に挿入」または「コピー」ボタンで使う

### 整形スタイルの切り替え
パネル上部のタブで切り替えられます。

| スタイル | 用途 |
|----------|------|
| 💬 カジュアル | チャット・SNS（デフォルト） |
| 📝 ビジネス | メール・報告書 |
| 📋 箇条書き | 議事録・リスト |
| 📊 要約 | 長い内容をまとめたいとき |

### 🧠 Thinking モード
ヘッダーの 🧠 ボタンをオンにすると、Claude が熟考してから整形します。
- **OFF（デフォルト）**: 約 3〜5 秒
- **ON**: 約 10〜30 秒（精度が上がる）

---

## 辞書機能

Whisper が誤認識しやすい単語を登録しておくと、文字起こし後に自動で修正されます。

### 登録方法
1. 拡張機能アイコン → VoiceInk のポップアップを開く
2. 辞書セクションで「誤認識される語」→「正しい語」を入力して「＋」

### バックアップ・引き継ぎ
- **書き出し**: ポップアップ下部の「書き出し」ボタン → `voiceink-dict.json` を保存
- **読み込み**: 「読み込み」ボタンで JSON ファイルを選択

> 📌 同じフォルダを使い続けて拡張機能を更新している限り、辞書データは自動で引き継がれます。

---

## パネルの操作

- **移動**: パネル上部のヘッダー部分をドラッグして好きな位置に移動できます
- **閉じる**: ×ボタン、または `Cmd+Shift+Y`

---

## よくあるトラブルと解決法

### 更新したのに変化がない
→ **ページを `Cmd+R` でリロードしましたか？** 拡張機能の更新だけではタブに反映されません。

### ショートカットキーが効かない
→ まず **使っているページを `Cmd+R` でリロード**してみてください。これで直ることが多いです。

→ それでも効かない場合は `chrome://extensions/shortcuts` を開き、キーが「未設定」になっていないか確認してください。なっている場合は鉛筆アイコンから手動で再設定します。
- 「VoiceInkを開く/閉じる」→ `Cmd+Shift+Y`
- 「録音開始/停止」→ `Cmd+Shift+K`

### 文字起こしに失敗する（Groq 401 エラー）
→ API キーが設定されていないか、期限切れです。ポップアップを開いて Groq API キーを再入力・保存してください。

### AI整形に失敗する（Claude エラー）
→ Anthropic API キーを確認してください。またはクレジット残高が不足している可能性があります。

### 音声入力後に文字が削除できない・送信できない（一部サイト）
→ Frame.io など特殊なエディタを使うサイトでは、「入力欄に挿入」ではなく「コピー」ボタンを使って `Cmd+V` で貼り付けてください。

### パネルが表示されない（chrome:// ページなど）
→ Chrome の内部ページ（`chrome://` から始まる URL）では拡張機能は動作しません。通常のウェブサイトで使用してください。

### AI整形結果がおかしい（ルール説明が出てくるなど）
→ 古いバージョンが動いている可能性があります。更新手順（Pull origin → 更新ボタン → `Cmd+R`）をやり直してください。

---

## 辞書データについて

- 辞書は Chrome の `chrome.storage.sync` に保存されます
- **同じフォルダを使い続ける限りデータは消えません**
- 拡張機能を削除して別のフォルダから再インストールするとデータが消えます
- 大切な辞書データは定期的に「書き出し」でバックアップを取ることをおすすめします

---

## 技術情報

| 項目 | 内容 |
|------|------|
| 文字起こし | Groq API（whisper-large-v3） |
| AI整形 | Anthropic Claude API（claude-sonnet-4-5） |
| Web版 | Vercel（自動デプロイ） |
| リポジトリ | `daikisubad-bit/voice-ink` |
